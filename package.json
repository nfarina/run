{
  "name": "fast-run",
  "version": "1.0.8",
  "description": "Executes scripts in your package.json much faster than NPM or Yarn, especially for projects using Yarn Workspaces. And with less typing.",
  "author": "nfarina@gmail.com",
  "license": "ISC",
  "bin": {
    "run": "bin/run.js"
  },
  "files": [
    "bin",
    "src/**/*.js"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "chalk": "4.x.x",
    "glob-promise": "^4.2.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "prettier": "^2.6.0",
    "typescript": "^4.6.2"
  }
}
